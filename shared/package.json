{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "description": "טיפוסים ומודלים משותפים בין ה-frontend וה-backend (dudu-move-site-builder)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
